# goto main.py and goto the top line and paste
from discord.ui import Button, View

view=View()
View.add_item(Button)
button = Button(label="Go Futher In the     Future", style= discord.buttonstyle.danger)